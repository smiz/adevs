# Common macros
COMPARE = tclsh $(PREFIX)/util/compare
# GNU gcc compiler
CFLAGS = -fopenmp -g -Wall -I$(PREFIX)/include
CC = g++
TEST_EXEC=./a.out
DEL = rm -f
LIBS = -L$(PREFIX)/src -ladevs -lrt 
CMD_SEP = ;
# Microsoft cl compiler
#CFLAGS = /W4 /GR /EHsc /I$(PREFIX)/include /Fe$(TEST_EXEC)
#CC = cl
#TEST_EXEC = a.exe
#DEL = del
#LIBS = -L$(PREFIX)/src -ladevs
#CMD_SEP = &&
# Common targets
clean:
	$(DEL) *.obj *.o tmp $(TEST_EXEC)
